# 기존 HAProxy 설정 파일에 필요한 부분만 참고해서 추가하세요.
# 8095로 들어온 요청을 내부 웹앱 8094로 넘기는 예시입니다.

frontend camp_frontend
  bind *:8095
  mode http
  option httplog
  default_backend camp_backend

backend camp_backend
  mode http
  option httpchk GET /api/health
  server camp_app 127.0.0.1:8094 check
