const calendarGrid=document.querySelector('#calendarGrid');const currentMonthLabel=document.querySelector('#currentMonth');const prevMonthButton=document.querySelector('#prevMonth');const nextMonthButton=document.querySelector('#nextMonth');let visibleDate=new Date();visibleDate.setDate(1);const inventory=[{key:'캠핑',label:'캠핑',total:58},{key:'카라반',label:'카라반',total:6},{key:'난로대여',label:'난로대여',total:4}];function toDateKey(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}function touches(r,day){return r.startDate<=day&&r.endDate>=day}function uses(r,item){const e=r.equipment||[];if(item.key==='캠핑')return true;if(item.key==='카라반')return e.some(x=>x.includes('카라반'));if(item.key==='난로대여')return e.some(x=>x.includes('난로')||x.includes('랜턴')||x.includes('전원'));return false}async function fetchReservations(d){const y=d.getFullYear();const m=String(d.getMonth()+1).padStart(2,'0');const res=await fetch(`http://ccymproxmox.iptime.org:8094/api/reservations?month=${y}-${m}`);if(!res.ok)throw new Error('예약 정보를 불러오지 못했습니다.');return res.json()}function renderCalendar(reservations){const y=visibleDate.getFullYear();const m=visibleDate.getMonth();const first=new Date(y,m,1);const last=new Date(y,m+1,0);const cells=[];currentMonthLabel.innerHTML=`<span>${y}</span> 년 <b>${m+1}</b> 월`;['일요일','월요일','화요일','수요일','목요일','금요일','토요일'].forEach((d,i)=>cells.push(`<div class="calendar-weekday ${i===0?'sunday':i===6?'saturday':''}">${d}</div>`));for(let i=0;i<first.getDay();i++)cells.push('<div class="calendar-day muted"></div>');for(let day=1;day<=last.getDate();day++){const date=new Date(y,m,day);const key=toDateKey(date);const dayReservations=reservations.filter(r=>touches(r,key));const items=inventory.map(item=>{const matched=dayReservations.filter(r=>uses(r,item));const confirmed=matched.filter(r=>r.status==='confirmed'||r.paymentStatus==='paid').length;const pending=matched.filter(r=>r.status==='pending'&&r.paymentStatus!=='paid').length;const remains=Math.max(0,item.total-confirmed-pending);const state=confirmed>0?'confirmed':pending>0?'pending':'available';return `<a class="calendar-item ${state}" href="reserve.html?date=${key}&item=${encodeURIComponent(item.key)}"><span class="doc-icon">▤</span>${item.label}(${remains})</a>`}).join('');const type=date.getDay()===0?'sunday':date.getDay()===6?'saturday':'';cells.push(`<div class="calendar-day ${type}"><strong class="day-number">${day}</strong><div class="reservation-list">${items}</div></div>`)}calendarGrid.innerHTML=cells.join('')}async function refreshCalendar(){calendarGrid.innerHTML='<div class="calendar-loading">예약 정보를 불러오는 중입니다.</div>';try{renderCalendar(await fetchReservations(visibleDate))}catch{renderCalendar([])}}prevMonthButton.addEventListener('click',()=>{visibleDate=new Date(visibleDate.getFullYear(),visibleDate.getMonth()-1,1);refreshCalendar()});nextMonthButton.addEventListener('click',()=>{visibleDate=new Date(visibleDate.getFullYear(),visibleDate.getMonth()+1,1);refreshCalendar()});refreshCalendar();
